# SI Core

## Workspace CLI

Use the workspace helper to scaffold new packages from the templates stored in `manifests/templates`:

```bash
pnpm install
pnpm si new amazing-package
```

The generator creates a new package in `packages/amazing-package` with files rendered from the manifest templates. Update those templates to customize future scaffolding runs.

