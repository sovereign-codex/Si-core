{
  "name": "sovereign-intelligence",
  "version": "0.1.0",
  "private": true,
  "description": "Monorepo workspace for Sovereign Codex",
  "packageManager": "pnpm@8.15.8",
  "scripts": {
    "build": "pnpm -r run build",
    "lint": "pnpm -r run lint",
    "test": "pnpm -r run test",
    "typecheck": "pnpm -r run typecheck --if-present",
    "docs:generate": "node scripts/generate-docs.mjs",
    "codex:watch": "node scripts/codex-watch.mjs",
    "si": "node scripts/si.mjs",
    "codex:sync-environments": "node scripts/sync-codex-environments.mjs",
    "codex:propagate-ci": "node scripts/propagate-ci-workflow.mjs"
  },
  "devDependencies": {
    "node-fetch": "^3.3.2"
  }
}
